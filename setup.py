from setuptools import setup

setup(
    name='qtile-widget-scriptexit',
    packages=['scriptexit'],
    version='0.1.0',
    description='Qtile QuickExit widget but can run a script before exiting.',
    author='elParaguayo',
    url='https://github.com/elParaguayo/qtile-widget-scriptexit',
    license='MIT',
    install_requires=['qtile>0.14.2']
)
